<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tracker</title>

    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            overflow: hidden;
            /* Prevent scrollbar due to fixed position background */
        }

        .slideshow-container {
            width: 100%;
            height: 100%;
            position: fixed;
            top: 0;
            left: 0;
            z-index: -1;
            /* Move the slideshow behind the other elements */
        }

        .fade {
            width: 100%;
            height: 100%;
            object-fit: cover;
            animation: fade 4s infinite;
        }

        header {
            background-color: rgba(51, 51, 51, 0.7);
            color: white;
            text-align: center;
            padding: 20px;
            position: relative;
            z-index: 1;
            /* Ensure the header stays on top of the slideshow */
        }

        h1 {
            color: white;
            text-align: center;
        }

        ul {
            list-style-type: none;
            padding: 0;
            margin: 20px 0;
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            position: relative;
            z-index: 2;
            /* Ensure the activity list stays on top of the slideshow */
        }

        li {
            background-color: #fff;
            border: 1px solid #ccc;
            border-radius: 5px;
            margin: 10px;
            padding: 20px;
            text-align: center;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        li:hover {
            background-color: #ddd;
        }

        @keyframes fade {
            from {
                opacity: .4;
            }

            to {
                opacity: 1;
            }
        }
    </style>
</head>

<body>

    <!-- Background Slideshow -->
    <div class="slideshow-container">
        <img class="fade" src="/Images/tracker-image1.jpg" alt="Tracker Background 1">
        <img class="fade" src="/Images/tracker-image2.jpg" alt="Tracker Background 2">
        <img class="fade" src="/Images/tracker-image3.jpg" alt="Tracker Background 3">
        <!-- Add more images as needed -->
    </div>

    <header>
        <h1>Know About Yourself</h1>
    </header>

    <ul>
        <li onclick="location.href='/watertracker';">Water</li>
        <li onclick="location.href='/weight';">Weight</li>
        <li onclick="location.href='/calorie';">Calorie</li>
    </ul>

    <script>
        let slideIndex = 0;

        function showSlides() {
            const slides = document.querySelectorAll('.fade');

            for (let i = 0; i < slides.length; i++) {
                slides[i].style.display = 'none';
            }

            slideIndex++;

            if (slideIndex > slides.length) {
                slideIndex = 1;
            }

            slides[slideIndex - 1].style.display = 'block';
            setTimeout(showSlides, 4000);
        }

        showSlides();
    </script>

</body>

</html>